using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProceduralAnimationManager : MonoBehaviour
{

    // two transforms , lhip rhip

    // ray cast down, FixedUpdateevery frame
    // calculate distance bteween foot and ray
    // lerp the ankle to the RC hitpos

    [SerializeField] private Transform leftHip;
    [SerializeField] private Transform righttHip;

    // attach the foot to the floor



}
